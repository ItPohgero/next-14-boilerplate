'use client'
import React, { PropsWithChildren } from 'react'

/**
 * @description
 *  Generate by itpohgero
 *  You can modify this layout to custom ui
 */
const LayoutAbout = (props: PropsWithChildren) => {
    return (
        <main>{props?.children}</main>
    )
}

export default LayoutAbout
