import React, { PropsWithChildren } from 'react'
import LayoutAbout from '@/layouts/LayoutAbout'

/**
 * @description
 *  Generate by itpohgero
 *  You can see you layout in layout path
 */
const Layout = ({ children }: PropsWithChildren) => <LayoutAbout>{children}</LayoutAbout>
export default Layout

